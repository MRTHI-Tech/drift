{
  "name": "drift",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @drift/dashboard dev",
    "build": "pnpm -r --sequential build",
    "typecheck": "pnpm -r --sequential typecheck",
    "test": "pnpm -r --sequential test",
    "worker": "pnpm --filter @drift/worker start",
    "seed": "pnpm --filter @drift/core exec tsx --env-file-if-exists=../../.env.local scripts/seed.ts",
    "prune-findings": "pnpm --filter @drift/core exec tsx --env-file-if-exists=../../.env.local scripts/prune-findings.ts",
    "adopt-projects": "pnpm --filter @drift/core exec tsx --env-file-if-exists=../../.env.local scripts/adopt-projects.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
