/**
 * The only place model IDs are written down. Never inline a model ID anywhere
 * else (AGENTS.md section 1).
 */

/** Text and vision model behind every Genkit flow. */
export const GEMINI_MODEL = "gemini-3.5-flash"

/** Embedding model used to place screens near their archetype siblings. */
export const GEMINI_EMBEDDING_MODEL = "gemini-embedding-2"
